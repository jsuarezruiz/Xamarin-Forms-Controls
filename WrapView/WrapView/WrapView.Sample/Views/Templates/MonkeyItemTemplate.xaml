<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="WrapViewSample.Views.Templates.MonkeyItemTemplate">
    <ContentView.Content>
        <Grid
            HeightRequest="200"
            WidthRequest="200">
            <AbsoluteLayout>
                <Image 
                    Grid.Row="0"
                    Source="{Binding Image}"
                    Aspect="AspectFill"
                    AbsoluteLayout.LayoutFlags="All"                               
                    AbsoluteLayout.LayoutBounds="0, 0, 1.0, 1.0" />
            </AbsoluteLayout>
            <Label 
                Text="{Binding Name}"
                TextColor="White"
                FontSize="Large"
                HorizontalOptions="Center"
                VerticalOptions="EndAndExpand"/>
        </Grid>
    </ContentView.Content>
</ContentView>
